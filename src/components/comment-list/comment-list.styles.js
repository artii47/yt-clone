import styled from "styled-components";
import { SpinnerOuter } from "../spinner/spinner.styles";

export const CommentList = styled.div`
  display: flex;
  flex-wrap: wrap;
`;

export const CommentListSpinner = styled(SpinnerOuter)``;
